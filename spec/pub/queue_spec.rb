require "spec_helper"

module Pub
  describe Queue do
    describe "#pop" do
      it "pops a specified number of queued requests"
    end

    describe "#publish" do
      it "publishes a response to a queued request"
    end

    describe "#subscribe" do
      it "queues a request"

      it "subscribes to its response"

      it "unsubscribes after receiving the response"
    end
  end
end
